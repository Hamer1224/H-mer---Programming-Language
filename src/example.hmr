Get math
-- Using the ?<%N> Chaos Operator (Requires math.seed)
if ?<%40> is
    print ">> Success: Luck Roll hit (40%)"
    success_count = success_count + 1
done

attempts = attempts - 1

@intel is
    mov rax, 1     
    nop             
done
