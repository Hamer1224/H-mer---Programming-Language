GET math
local math = new MathLib
local total = 50
local wins = 0

print "Running 50 Probability Tests..."

while total > 0 do
    if ?<%30> is
        wins = wins + 1
    done
    total = total - 1
done

print "Total Winners:"
print wins
